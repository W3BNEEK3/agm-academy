name: Build and Deploy to cPanel server for agm-academy.com

on:
  push:
    branches:
      - main
  workflow_dispatch:

env:
  NODE_VERSION: 22
  TAR_NAME: agm-academy-prod.tar.gz
  UPLOAD_NODE_MODULES: ${{ secrets.UPLOAD_NODE_MODULES || 'false' }}

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Use Node.js ${{ env.NODE_VERSION }}
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Build
        run: npm run build

      - name: Create production tarball
        run: |
          rm -f $TAR_NAME
          if [ "${{ env.UPLOAD_NODE_MODULES }}" = "true" ]; then
            tar -czf $TAR_NAME .next node_modules public package.json next.config.ts package-lock.json
          else
            tar -czf $TAR_NAME .next public package.json next.config.ts package-lock.json
          fi
        env:
          TAR_NAME: ${{ env.TAR_NAME }}

      - name: Verify tarball contents
        run: |
          ls -lh $TAR_NAME
          echo "node_modules entries:"
          tar -tzf $TAR_NAME | grep node_modules | wc -l || true
        env:
          TAR_NAME: ${{ env.TAR_NAME }}

      - name: Copy tarball to server
        uses: appleboy/scp-action@v0.1.4
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USER }}
          port: ${{ secrets.SSH_PORT }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          source: ${{ env.TAR_NAME }}
          target: /home/${{ secrets.SSH_USER }}/

      - name: Extract on server and deploy
        uses: appleboy/ssh-action@v0.1.8
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USER }}
          port: ${{ secrets.SSH_PORT }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          script: |
            set -e
            TARGET_DIR='${{ secrets.TARGET_DIR }}'
            BACKUP_DIR="${TARGET_DIR}.bak.$(date +%s)"

            # Preserve .htaccess across deploys
            if [ -f "$TARGET_DIR/.htaccess" ]; then
              cp "$TARGET_DIR/.htaccess" /tmp/htaccess.bak
            fi

            # Move current app to a fresh backup
            if [ -d "$TARGET_DIR" ]; then
              mv "$TARGET_DIR" "$BACKUP_DIR"
            fi
            mkdir -p "$TARGET_DIR"
            tar -xzf /home/${{ secrets.SSH_USER }}/${{ env.TAR_NAME }} -C "$TARGET_DIR"
            chown -R ${{ secrets.SSH_USER }}:${{ secrets.SSH_USER }} "$TARGET_DIR"

            # Restore .htaccess
            if [ -f /tmp/htaccess.bak ]; then
              cp /tmp/htaccess.bak "$TARGET_DIR/.htaccess"
            elif [ -f "$BACKUP_DIR/.htaccess" ]; then
              cp "$BACKUP_DIR/.htaccess" "$TARGET_DIR/.htaccess"
            fi

            NPM_BIN="/home/${{ secrets.SSH_USER }}/nodevenv/$(basename $TARGET_DIR)/22/bin/npm"

            # Check node_modules is actually populated, not just present
            MODULE_COUNT=0
            if [ -d "$TARGET_DIR/node_modules" ]; then
              MODULE_COUNT=$(find "$TARGET_DIR/node_modules" -mindepth 1 -maxdepth 1 | wc -l)
            fi

            if [ "$MODULE_COUNT" -lt 10 ] && [ -x "$NPM_BIN" ]; then
              echo "node_modules missing or incomplete ($MODULE_COUNT entries) — running npm install"
              (cd "$TARGET_DIR" && "$NPM_BIN" install --omit=dev)
            fi

            # Restart via Passenger
            mkdir -p "$TARGET_DIR/tmp"
            touch "$TARGET_DIR/tmp/restart.txt"

            # Keep only the most recent backup to avoid disk/I-O buildup
            ls -dt "${TARGET_DIR}".bak.* 2>/dev/null | tail -n +2 | xargs -r rm -rf
